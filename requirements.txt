# Core dependencies
langgraph>=0.2.0
langchain>=0.3.0
langchain-groq>=0.2.0
pydantic>=2.0.0
groq>=0.11.0

# Checkpointing (enables --resume). Optional in the sense that main.py
# degrades gracefully if this isn't installed or its API doesn't match -
# see _build_checkpointer() in main.py. Verify against your installed
# langgraph version; this package's API has shifted across releases.
langgraph-checkpoint-sqlite>=1.0.0

# GitHub integration
PyGithub>=2.4.0

# Code analysis and formatting
ruff>=0.7.0
black>=24.0.0
mypy>=1.13.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0

# CLI
click>=8.1.0
python-dotenv>=1.0.0

# Utilities
rich>=13.9.0
tenacity>=9.0.0